import 'package:flutter/material.dart';

class BusTwo extends StatelessWidget {
  const BusTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

        // body: SlidingUpPanel(minHeight: 100, maxHeight: 600)
        );
  }
}
